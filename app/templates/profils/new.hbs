<h3>Ajout de profil</h3>
<div class="segment">
  <form>
    <div class="form-group">
      <label for="name">Nom :</label>
      {{input type="text" id="name" class="form-control"
              placeholder="Nom du profil" value=model.name}}
    </div>
    <button {{action "save" model}} class="btn btn-primary">✓ Enregistrer</button>
    {{#link-to "profils" class="btn btn-default"}}Annuler{{/link-to}}
  </form>
</div>
{{outlet}}